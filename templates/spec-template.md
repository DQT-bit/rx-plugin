# <Rx 编号> <简短标题>

## 项目根目录
```
PROJECT_ROOT=/绝对路径/到项目根
```
所有"涉及文件"的相对路径基于此解析。

## 目标
一句话说清做什么。后续每一段都为这句话服务。

## 项目上下文（reasonix 容易翻车的点）
- 技术栈 / 语言版本（如 Java 17、Spring Boot 3、Vue 3）
- 包名约定、方法签名约定、框架惯例
- 注释语言

## 参考实现（动手前 Read）
- `/绝对路径/SomeService.java` — 同模式的参考写法

> ⚠️ reasonix 的文件沙箱限定在项目根内 —— 项目外的参考文件要么把全文贴进本规格，要么先 `cp` 进项目根再引用。

## 涉及文件（绝对路径）
**新建（N 个）：**
1. `/绝对路径/X`

**修改（M 个）：**
2. `/绝对路径/Y`（仅在某区域改 N 行）

## 改动指令
针对**每个文件**写**具体内容**：
- 简单的（DTO / 配置）：直接贴完整内容
- 复杂的（算法 / 状态机）：贴**完整伪代码** + 关键注释
- 修改型：贴 before/after 片段

## 完成判据（机器可验证）
每条都要能跑命令验证：
- `cd <根> && <编译命令>` 退出码 0
- `<运行 / 调用命令>` 返回预期结果

> ❌ 禁止主观判据（"代码正确"、"逻辑清晰"）—— reasonix 无法自查。
> ⚠️ 判据要**精确匹配意图** —— 别用一刀切的检查（如 `grep -c X = 0` 这种"一个都不剩"），reasonix 会为满足判据做过头（比如把功能性代码也一并删掉）。

## 不得改动
显式列出不能碰的文件 / 目录，越具体越好。

## 翻车点
针对本任务的特定提醒（关键字命名、事务回滚、边界 case 等）。

<!-- rx-budget: 0.10   ← 可选：写了 rx-go 自动读为预算，省略则按规格行数估算 -->
